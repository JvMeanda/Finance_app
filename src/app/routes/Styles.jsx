import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    labelStyle: {
        fontWeight: 'bold',
        fontSize: 12,
        marginBottom: 5,
    },
    barStyle: {
        paddingVertical: 10,
        height: 60,
        justifyContent: 'center',
        alignItems: 'center',
    },
    iconStyle: {
        marginTop: 0,
    }
});
